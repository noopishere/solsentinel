# SolSentinel Configuration

# API server port
PORT=3000

# Solana RPC URL (devnet by default)
SOLANA_RPC_URL=https://api.devnet.solana.com

# Path to Solana wallet keypair (for on-chain uploads)
SOLANA_WALLET=~/.config/solana/id.json

# Crawl interval in milliseconds (default: 1 hour)
CRAWL_INTERVAL_MS=3600000

# Environment mode (development / production)
NODE_ENV=development
